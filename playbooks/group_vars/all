---

# Here we define global variables for our server group, but if some servers
# require custom values place these variables in /etc/ansible/hosts to override
# for each individual host

# ome credentials (example)
omeuser: <user>
omepswd: <password>

# Local directory where all results are placed
# rootdir: /root

# Refers to the hostname, not the OOB controller
host: "{{inventory_hostname}}"

# Misc
emailRecipient: <mailRecipient>

# Discovery
discStartAddress: 192.168.188.67 # start adress for discovery
discEndAddress: 192.168.188.67 # end address for discovery


